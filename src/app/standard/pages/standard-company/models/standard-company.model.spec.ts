import { StandardCompany } from './standard-company.model';

describe('StandardCompany', () => {
  it('should create an instance', () => {
    expect(new StandardCompany()).toBeTruthy();
  });
});
