import { StandardRegister } from './standard-register.model';

describe('StandardRegister', () => {
  it('should create an instance', () => {
    expect(new StandardRegister()).toBeTruthy();
  });
});
