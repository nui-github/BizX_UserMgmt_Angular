import { StandardSubdistrict } from './standard-subdistrict.model';

describe('StandardSubdistrict', () => {
  it('should create an instance', () => {
    expect(new StandardSubdistrict()).toBeTruthy();
  });
});
