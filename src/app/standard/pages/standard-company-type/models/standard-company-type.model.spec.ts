import { StandardCompanyType } from './standard-company-type.model';

describe('StandardCompanyType', () => {
  it('should create an instance', () => {
    expect(new StandardCompanyType()).toBeTruthy();
  });
});
