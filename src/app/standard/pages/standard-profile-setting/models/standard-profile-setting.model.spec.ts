import { StandardProfileSetting } from './standard-profile-setting.model';

describe('StandardProfileSetting', () => {
  it('should create an instance', () => {
    expect(new StandardProfileSetting()).toBeTruthy();
  });
});
