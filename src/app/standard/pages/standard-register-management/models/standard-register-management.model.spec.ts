import { StandardRegisterManagement } from './standard-register-management.model';

describe('StandardRegisterManagement', () => {
  it('should create an instance', () => {
    expect(new StandardRegisterManagement()).toBeTruthy();
  });
});
