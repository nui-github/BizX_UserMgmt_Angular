import { StandardMainmenu } from './standard-mainmenu.model';

describe('StandardMainmenu', () => {
  it('should create an instance', () => {
    expect(new StandardMainmenu()).toBeTruthy();
  });
});
