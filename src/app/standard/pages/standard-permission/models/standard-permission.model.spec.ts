import { StandardPermission } from './standard-permission.model';

describe('StandardPermission', () => {
  it('should create an instance', () => {
    expect(new StandardPermission()).toBeTruthy();
  });
});
