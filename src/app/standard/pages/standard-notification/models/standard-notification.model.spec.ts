import { StandardNotification } from './standard-notification.model';

describe('StandardNotification', () => {
  it('should create an instance', () => {
    expect(new StandardNotification()).toBeTruthy();
  });
});
