import { StandardFilterPipe } from './filter.pipe';

describe('StandardFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StandardFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
